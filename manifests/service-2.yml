apiVersion: v1
kind: Service
metadata:
    name: "clusterf-b59b"
    labels:
        app: "clusterf-b59b"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "clusterf-b59b"