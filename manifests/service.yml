apiVersion: v1
kind: Service
metadata:
    name: "eiriktestcluster-e161"
    labels:
        app: "eiriktestcluster-e161"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "eiriktestcluster-e161"